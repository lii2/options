-- Table: public.ema_data

-- DROP TABLE public.ema_data;

CREATE TABLE public.ema_data
(
    day date NOT NULL,
    ticker character varying(50) COLLATE pg_catalog."default" NOT NULL,
    ema numeric,
    CONSTRAINT "EmaData_pkey" PRIMARY KEY (ticker, day)
)
WITH (
    OIDS = FALSE
)
TABLESPACE pg_default;

ALTER TABLE public.ema_data
    OWNER to postgres;

------------------------

-- Table: public.stock_data

-- DROP TABLE public.stock_data;

CREATE TABLE public.stock_data
(
    day date NOT NULL,
    ticker character varying(50) COLLATE pg_catalog."default" NOT NULL,
    open numeric,
    close numeric,
    high numeric,
    low numeric,
    volume numeric,
    CONSTRAINT stock_data_pkey PRIMARY KEY (day, ticker)
)
WITH (
    OIDS = FALSE
)
TABLESPACE pg_default;

ALTER TABLE public.stock_data
    OWNER to postgres;


------------------

-- Table: public.macd_data

-- DROP TABLE public.macd_data;

CREATE TABLE public.macd_data
(
    ticker character varying(50) COLLATE pg_catalog."default" NOT NULL,
    day date NOT NULL,
    macd numeric,
    macd_hist numeric,
    macd_signal numeric,
    CONSTRAINT macd_data_pkey PRIMARY KEY (ticker, day)
)
WITH (
    OIDS = FALSE
)
TABLESPACE pg_default;

ALTER TABLE public.macd_data
    OWNER to postgres;

 -------------------

 -- Table: public.bband_data

 -- DROP TABLE public.bband_data;

 CREATE TABLE public.bband_data
 (
     day date NOT NULL,
     ticker character varying(50) COLLATE pg_catalog."default" NOT NULL,
     real_lower_band numeric(50,0),
     real_upper_band numeric(50,0),
     real_middle_band numeric(50,0),
     CONSTRAINT bband_data_pkey PRIMARY KEY (day, ticker)
 )
 WITH (
     OIDS = FALSE
 )
 TABLESPACE pg_default;

 ALTER TABLE public.bband_data
     OWNER to postgres;